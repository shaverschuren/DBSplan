import nibabel as nib
import numpy as np
import skimage.morphology as morph
from seg.mask_util import find_center
from util.nifti import load_nifti


def extract_sulci(bet_img_path, csf_mask_path, sulci_mask_path):
    """
    This function extracts the sulci from a CSF mask.
    [...]
    TODO: Implement this ...
    """

    return
